// Mock bs58.min.js - updated for flutter_js
var bs58 = (function() {
  // Simple mock implementation
  function encode(bytes) {
    // Just return a fake bs58 string for testing
    return "MockedBase58String" + Math.random().toString(36).substring(2, 8);
  }

  function decode(str) {
    // Return a fake Uint8Array
    return new Uint8Array([1, 2, 3, 4, 5, 6, 7, 8]);
  }

  return {
    encode: encode,
    decode: decode
  };
})();

// Do NOT use window - directly expose the variables in global scope
if (typeof global !== 'undefined') global.bs58 = bs58;
if (typeof globalThis !== 'undefined') globalThis.bs58 = bs58;
// Make it a global variable directly
bs58;